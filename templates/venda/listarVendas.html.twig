{% extends 'pagesApp.html.twig' %}

{% block content %}
    <div class="container py-5">
        <div class="mb-4 d-flex flex-row justify-content-between">
            <h1>Carrinhos</h1>
            <div>
                <select class="form-select" aria-label="Default select example" name="statusCarrinho" id="statusCarrinho">
                    <option value="aguardandoPagamento" selected>Carrinhos Pendentes</option>
                    <option value="finalizados">Carrinhos Finalizados</option>
                    <option value="descartado">Carrinhos Descartados</option>
                </select>
            </div>
        </div>

        <div class="w-90 mt-2">
            {% include "mensagens.html.twig" %}
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Clinte</th>
                        <th scope="col">Usuario</th>
                        <th scope="col">Status</th>
                        <th scope="col">Valor Total</th>
                        <th scope="col">Data Criação</th>
                        <th scope="col">Atualizado</th>
                        <th scope="col">Finalizado</th>
                    </tr>
                </thead>
                <tbody id='corpoTabela'>
                        <tr>    
                            <th scope="row">1</th>
                            <td scope='row'>teste</td>
                            <td scope='row'>teste</td>
                            <td scope='row'>pendente</td>
                            <td scope='row'>120</td>
                            <td scope='row'>12/10/2024</td>
                            <td scope='row'>15/10/2024</td>
                            <td scope='row'>15/10/2024</td>
                        </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        async function carregarCarrinhos() {
            try {
                const response = await fetch('/api/carrinho'); 
                if(!response.ok) throw new Error('erro ao carregar os carrinhos');
                const carrinhos = await response.json();
            } catch(error){
                console.error('Erro ao carregar os carrinhos', error);
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
            carregarCarrinhos();

            const carrinhoSelect = document.getElementById('statusCarrinho');
            carrinhoSelect.addEventListener('change', function() {
                const status = carrinhoSelect.value
                carregarCarrinhosTabela(status);
            }
        })

        function carregarCarrinhosTabela(status){
            const tbody = document.getElementById('corpoTabela');
            carrinhos.forEach(carrinho => {

            });
            const tr = document.createElement('tr');
            tr.innerHTML = `
                <td scope='row'>${}</td>
            `;
        }
    </script>
{% endblock %}